@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>Learn about <a href="https://learn.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
</div>

<div class="mt-4">
    <h2>Demos</h2>
    <div class="list-group">
        <a asp-controller="@(Name<ToastController>.Value)" asp-action="@nameof(ToastController.Index)" class="list-group-item list-group-item-action">
            <div class="d-flex w-100 justify-content-between">
                <h5 class="mb-1">Bootstrap Toast Messages</h5>
            </div>
            <p class="mb-1">Interactive demo for creating and customizing Bootstrap toast notifications.</p>
        </a>
        <a asp-controller="@(Name<ToastController>.Value)" asp-action="@nameof(ToastController.FormExample)" class="list-group-item list-group-item-action">
            <div class="d-flex w-100 justify-content-between">
                <h5 class="mb-1">Form POST with Toast Messages</h5>
            </div>
            <p class="mb-1">Traditional form submission using Post-Redirect-Get pattern with TempData to display toast notifications.</p>
        </a>
        <a asp-controller="@(Name<ExampleRecordController>.Value)" asp-action="@nameof(ExampleRecordController.Index)" class="list-group-item list-group-item-action">
            <div class="d-flex w-100 justify-content-between">
                <h5 class="mb-1">Repository Pattern with Pagination (Traditional)</h5>
            </div>
            <p class="mb-1">Server-side pagination demonstrating the Repository pattern with Unit of Work. Full page reload with numbered page links.</p>
        </a>
        <a asp-controller="@(Name<ExampleRecordController>.Value)" asp-action="@nameof(ExampleRecordController.AjaxExample)" class="list-group-item list-group-item-action">
            <div class="d-flex w-100 justify-content-between">
                <h5 class="mb-1">Repository Pattern with Pagination (AJAX)</h5>
            </div>
            <p class="mb-1">Modern client-side pagination using fetch API with URL state management. Data loaded dynamically without page refresh. Supports bookmarks, back button, and page refresh.</p>
        </a>
        <a asp-controller="@(Name<ExampleRecordController>.Value)" asp-action="@nameof(ExampleRecordController.CacheExample)" class="list-group-item list-group-item-action">
            <div class="d-flex w-100 justify-content-between">
                <h5 class="mb-1">Entity Cache Pattern</h5>
            </div>
            <p class="mb-1">Demonstrates EntityCache for merging multiple query results with automatic deduplication. Shows type-safe key handling and orchestration patterns.</p>
        </a>
        <a asp-controller="@(Name<DeleteConfirmationController>.Value)" asp-action="@nameof(DeleteConfirmationController.Index)" class="list-group-item list-group-item-action">
            <div class="d-flex w-100 justify-content-between">
                <h5 class="mb-1">Delete Confirmation (Inline Popovers)</h5>
            </div>
            <p class="mb-1">Inline delete confirmations using Bootstrap popovers. "Flip cover" pattern with spatial proximity - better UX than modal dialogs.</p>
        </a>
        <a asp-controller="@(Name<DisableController>.Value)" asp-action="@nameof(DisableController.Index)" class="list-group-item list-group-item-action">
            <div class="d-flex w-100 justify-content-between">
                <h5 class="mb-1">Button State Management</h5>
            </div>
            <p class="mb-1">Disable/enable buttons and show loading states during async operations. Custom loading text via data attributes with automatic content preservation.</p>
        </a>
    </div>
</div>
